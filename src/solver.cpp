// #include "solver.h"

// GurobiSolver::GurobiSolver() : model(nullptr) { GRBloadenv(&env, nullptr); }
// GurobiSolver::GurobiSolver(GRBenv *_env) : env(_env), model(nullptr) {}

// GurobiSolver::~GurobiSolver()
// {
//     if (model != nullptr)
//         GRBfreemodel(model);
//     if (env != nullptr)
//         GRBfreeenv(env);
// }

// void GurobiSolver::set_template(const StageProblem &stage_problem)
// {
//     if (model != nullptr)
//         GRBfreemodel(model);


// }
