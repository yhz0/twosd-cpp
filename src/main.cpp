#include <iostream>
#include <omp.h>

// #include "two_stage.h"

int main() {

    // int nworkers = omp_get_max_threads();

    // TwoStageSCS sp("tests", "ssn", nworkers);

    // // TwoStageSCS sp("tests", "lands", nworkers);
    // sp.solve();
    // return 0;
}

